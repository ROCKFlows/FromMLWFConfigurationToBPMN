type Query {
    configuration(name: ID!): Configuration
}

type Configuration {
    name: ID!
    features: [Feature!]!
}

enum FeatureSelectionStatus {
    selected,
    unselected,
    undefined,
}

type Feature {
    name: ID!
    automatic: FeatureSelectionStatus
    manual: FeatureSelectionStatus
}
