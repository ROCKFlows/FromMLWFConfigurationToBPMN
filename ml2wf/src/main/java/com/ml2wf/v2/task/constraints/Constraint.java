package com.ml2wf.v2.task.constraints;

public class Constraint {

    // TODO: check if we need to represent a constraint as a FMTask
    //  if so, then add abstraction between AbstractTask and FeatureModelTask/Constraint

    /*
    protected Constraint(Node node, Constraint parent, boolean isAbstract) {
        super(node, parent, isAbstract);
    }

    protected Constraint(Node node, Constraint parent) {
        super(node, parent);
    }

    protected Constraint(Node node, boolean isAbstract) {
        super(node, isAbstract);
    }

    protected Constraint(Node node) {
        super(node);
    }
    */
}
