package com.ml2wf.v2.tree;

import java.util.Iterator;

/**
 * An {@link Iterator} extension dedicated to tree iteration.
 *
 * <p>
 *
 * This interface does not provide other methods than the {@link Iterator} one.
 *
 * @param <T> the tree's leaves type
 *
 * @since 1.1.0
 */
public interface ITreeIterator<T> extends Iterator<T> {
}
